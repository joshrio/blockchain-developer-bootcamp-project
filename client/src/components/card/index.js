// Library Imports
import React from "react";

// Relative Imports
import { Container } from "./styles";

const Card = ({ player }) => {
  return <Container>{player}</Container>;
};

export default Card;
