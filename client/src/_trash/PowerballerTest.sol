pragma solidity >=0.5.0;

import "truffle/Assert.sol";
import "truffle/DeployedAddresses.sol";
import "../contracts/Powerballer.sol";

contract TestPowerballer {


  /* function getPlayers() public {
    Powerballer powerballer = Powerballer(DeployedAddresses.Powerballer());

    Powerballer.getPlayers();

    [] expected = players[];

    Assert.equal(Powerballer.getPlayers(), expected, "It should store return an array");
  } */

}
